{
  "compilerOptions": {
    "types": ["node"],
    "lib": ["ESNext"],
    "noImplicitReturns": true, // because this is a library
    "paths": {
    },
  },
  "include": [
    "**/*.ts",
  ],
  "exclude": ["node_modules", "dist"]
}
